[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "solis-modbus"
version = "0.1.0"
description = "Read and control Ginlong Solis hybrid inverters over Modbus."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Apache-2.0" }
authors = [{ name = "solis-modbus contributors" }]
keywords = ["modbus", "solis", "ginlong", "inverter", "solar", "home-assistant"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Topic :: Home Automation",
]
dependencies = ["modbus-connection[tmodbus]>=4.7.0"]

[project.urls]
Homepage = "https://github.com/balloobbot/solis-modbus"

[dependency-groups]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.24",
    "ruff>=0.14",
    "mypy>=1.14",
]

[tool.hatch.build.targets.wheel]
packages = ["src/solis_modbus"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
src = ["src"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]

[tool.mypy]
python_version = "3.12"
files = ["src", "tests"]
strict = true
